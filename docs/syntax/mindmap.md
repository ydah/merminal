# Mindmap syntax

`mindmap` accepts indented nodes. The first node is the root; indentation determines parentage. `id((label))`, `id(label)`, `id[label]`, and `id))label((` select circle, rounded, rectangle, and cloud frames. Other labels use a rectangle.
