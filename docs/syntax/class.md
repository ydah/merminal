# Class syntax

`classDiagram` accepts `class Name`, bodies in `{ ... }`, attributes, operations, and `Name : member` lines. Relations include inheritance `<|--`, realization `..|>`, composition `*--`, aggregation `o--`, dependency `..>`, association `-->`, plain `--`, and reverse forms. A label can follow `:`. Unsupported annotations are ignored.
