# ER syntax

`erDiagram` accepts `ENTITY { ... }` attribute blocks and relation lines such as `CUSTOMER ||--o{ ORDER : places`. Solid and dotted relationships are rendered; cardinalities are written as `1`, `0..1`, `1..*`, and `0..*` in the relation label.
