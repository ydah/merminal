# State syntax

`stateDiagram` and `stateDiagram-v2` accept `A --> B`, transition labels after `:`, `[*]` start and end, `state "Label" as ID`, `state ID <<choice>>`, `<<fork>>`, `<<join>>`, and `state ID { ... }` composite frames. `direction` accepts flowchart directions. Notes currently produce an information diagnostic and are omitted.
