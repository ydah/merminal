# Pie syntax

`pie` accepts `showData`, `title`, and quoted `"Label" : nonnegative-number` entries. Values are shown as horizontal proportional bars and percentages. `showData` adds the original values.
